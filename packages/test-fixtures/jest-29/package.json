{
  "name": "@spekra/test-fixture-jest-29",
  "version": "0.0.0",
  "private": true,
  "description": "Jest 29 test fixtures for @spekra/jest reporter testing",
  "scripts": {
    "clean": "rm -rf node_modules coverage pnpm-lock.yaml",
    "refresh": "pnpm clean && pnpm install",
    "test": "jest",
    "test:with-reporter": "jest --reporters=default --reporters=@spekra/jest"
  },
  "devDependencies": {
    "@spekra/jest": "workspace:*",
    "@types/jest": "30.0.0",
    "jest": "30.3.0",
    "ts-jest": "29.2.5",
    "typescript": "^5.3.0"
  }
}
