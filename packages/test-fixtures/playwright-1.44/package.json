{
  "name": "@spekra/test-fixture-pw-1.44",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules test-results pnpm-lock.yaml",
    "refresh": "pnpm clean && pnpm install",
    "postinstall": "playwright install chromium",
    "test": "playwright test",
    "typecheck:tests": "tsc --noEmit -p tsconfig.test.json"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "@spekra/playwright": "workspace:*",
    "typescript": "^5.3.0"
  }
}
